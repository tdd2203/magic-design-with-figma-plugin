---
description: Pair the Magic Design with Figma panel with this Claude Code session using the code the panel shows
argument-hint: <mã 6 số trong panel Figma>
---

The user wants to pair their Figma panel. The code they gave: "$ARGUMENTS".

If a code was given, call the `pair_figma` tool from the magic-design-with-figma plugin with it and tell the user the result. If no code was given, ask the user for the 6-digit code shown in the "Magic Design with Figma" panel in Figma (the plugin must be open and connected with its "Kết nối" button), then call `pair_figma`. Reply in the user's language.
